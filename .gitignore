# Python
__pycache__/
*.pyc

# 작업 메모는 로컬 전용 — todo.md·handoff 는 그 세션에서만 의미가 있다.
tasks/

# 채점 데이터는 로컬 전용 — 도구만 저장소에 둔다.
# 골든셋은 특정 저장소의 커밋·파일 경로에 묶여 있어 남의 환경에서 재현되지 않는다.
red-team/evals/
pr-triage/evals/*.json

# OS
.DS_Store

# Codex repository instructions are part of this distribution.
!AGENTS.md

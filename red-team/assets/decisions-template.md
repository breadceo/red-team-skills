# <gate>-<n> 처리 결과

## 반영 — 다음 컨텍스트의 `이미 반영된 지적` 이 된다
- [축] 파일:줄 요지 → 무엇을 했나 (커밋 해시)

## 후속 티켓 — 다음 컨텍스트의 `스코프 밖` 이 된다
- [축] 파일:줄 요지 → pre-existing. TICKET-XXX 신설 예정 / 원 티켓에 Relates

## 보류 — 사용자가 아직 결정하지 않음
- (없으면 비운다. 여기에 항목이 남아 있으면 라운드를 진행하지 않는다)
